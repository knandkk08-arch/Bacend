services:
  - type: web
    name: ezpay-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --threads 4 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
